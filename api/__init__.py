"""
VEDA localhost API package (M8-A).

Thin FastAPI adapter over the existing SAM engine — see api/app.py.
Product-facing identity is VEDA; SAM remains the internal engineering
and repository identity. This package must not contain business or
agent logic of its own.
"""
